ivan_geraldo@cloudshell:~/C_Language (myproject74067)$ as --version
GNU assembler (GNU Binutils for Ubuntu) 2.42
Copyright (C) 2024 Free Software Foundation, Inc.
This program is free software; you may redistribute it under the terms of
the GNU General Public License version 3 or later.
This program has absolutely no warranty.
This assembler was configured for a target of `x86_64-linux-gnu'.

ivan_geraldo@cloudshell:~/Assembly (myproject74067)$ as hello.s -o hello.o
hello.s: Assembler messages:
hello.s: Warning: end of file not at end of a line; newline inserted

ivan_geraldo@cloudshell:~/Assembly (myproject74067)$ ld hello.o -o hello
ld: warning: cannot find entry symbol _start; defaulting to 0000000000401000

ivan_geraldo@cloudshell:~/Assembly (myproject74067)$ ./hello 
Hello, World

2 WARNINGS QUE DERAM NA COMPILAÇÃO E NA LINKEDIÇÃO:

The Warning: end of file not at end of a line; newline inserted warning occurs because the GNU Assembler (gas) 
requires every assembly source file (typically .s or .S) to end with a blank newline character. 
If the final instruction or directive in your file sits on the very last line without a trailing line break, 
the compiler automatically inserts one to prevent syntax parsing issues and warns you about it. 

How to Fix ItOpen the source file or macro file generating the warning in your text editor.
Navigate to the very bottom of the file.Place your cursor at the end of the final line of 
code and press Enter to add a blank new line.Save the file and recompile. 
if you are generating assembly files programmatically via a script or command (like printf), 
ensure your string terminates with a \n newline character.
If you are using custom assembly macros, ensure that the macro definitions block closes 
properly and contains a final line break inside or immediately following the .endm directive

The linker warning cannot find entry symbol _start happens when the GNU linker (ld) 
cannot find the default entry point it looks for in an executable file. 
load-warning-cannot-find-entry-symbol-start)Why This HappensMissing _start label: 

The linker looks for a symbol named _start as the absolute beginning of program execution 
for an ELF binary.Using main instead: If you wrote a main function in assembly or used 
a C-style entry without standard C runtime startup files, ld cannot find _start.Default 
address fallback: The linker still builds the program, but it guesses a default 
memory address (0000000000401000) to start execution. 
load-warning-cannot-find-entry-symbol-start)How to Fix ItAdd _start to Assembly: 
Define and export the _start label in your assembly code so the linker finds it:

The errors you are encountering occur because your code is written in 32-bit x86 assembly (using pushl, %ebp, and 32-bit registers), but you are assembling it on a 64-bit system where the GNU Assembler (as) defaults to 64-bit mode (x86_64). In 64-bit mode, GAS does not recognize the l suffix for push instructions.

How to Fix the Error
You have two main options to resolve this:

Option 1: Compile as a 32-bit program (Recommended for this code)
If you want to keep the code as 32-bit assembly, you must explicitly instruct your assembler and linker to target 32-bit architecture.

Run the following commands in your terminal:

# Assemble with the 32-bit flag
as --32 test.S -o test.o

# Link with the 32-bit ELF emulation flag
ld -m elf_i386 test.o -o test

Option 2: Rewrite the code for 64-bit x86_64
If you are running a modern 64-bit system, it is usually better to write native 64-bit assembly. 
This requires changing pushl to pushq, using 64-bit registers (%rbp, %rsp), 
and updating the calling convention.

Bonus Code Fixes to Watch Out For
Once you fix the assembler flags, you will likely run into another minor bug in your 
code on line 33:

The Issue: At the top of your file, you declared .extern printf, but down in the function 
you called call _printf with an underscore. Standard ELF systems (like Linux) 
do not use a leading underscore for C functions.

The Fix: Change line 33 from call _printf to call printf.

ANOTACOES EBOOK : ProgrammingGroundUp-0-9
On x86 processors, there are several general-purpose registers which can be used with movl:
• %eax
• %ebx
• %ecx
• %edx
• %edi
• %esi
In addition to these general-purpose registers, there are also several special-purpose registers,
including:
• %ebp
• %esp
• %eip

